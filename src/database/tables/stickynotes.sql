CREATE TABLE stickynotes (note_id VARCHAR(255)PRIMARY KEY, title VARCHAR (255) NOT NULL,
 content VARCHAR(255) NOT NULL, created_at DATETIME2 NOT NULL)

DROP TABLE stickynotes

SELECT*FROM stickynotes;